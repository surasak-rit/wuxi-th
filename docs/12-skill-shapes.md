# 12 — รูปทรงพื้นที่โจมตีของวิชา (Skill AoE Shapes)

> ต่อยอด docs/11 §7 (Radial) และ docs/10 (skill schema) — นิยาม "ทรงพื้นที่" ของวิชาแต่ละท่า
> บนกริด ใช้กำหนด **ช่องที่โดนผล** และ **ระยะการโจมตี** ทุกทรงเป็น data-driven + ขยายตามระดับวิชาได้
>
> สัญลักษณ์แผนภาพ (กริดสี่เหลี่ยม default, มุมมองจากด้านบน): `C`=ตัวละครผู้ร่าย ·
> `#`=ช่องที่โดนผล · `.`=ช่องว่าง · "หน้า" = ทิศที่ผู้เล่นเล็ง (orientation)

---

## 1. หลักการรวม (Shape System)
- วิชาแต่ละท่ามีฟิลด์ `shape` = `{ type, params, orientation, scaling }`
  หรือ `shape_stages` (ถ้าทรง **เปลี่ยนชนิดตามระดับ** — §3.1); ตอน resolve เลือกทรงของระดับปัจจุบัน
  `active_shape = pick_stage(skill.shape_stages, skill.level) or skill.shape`
- เอนจินแปลง shape → **รายการ offset ช่อง** (เทียบจากตัวละคร) แล้ว:
  1. หมุนตาม `orientation` (สำหรับทรงมีทิศ) → ได้ช่องจริงบนกริด
  2. เก็บศัตรูทุกตัวที่ยืนบนช่องเหล่านั้น (ใช้ `grid_dist`/พิกัดช่อง — docs/11 §3)
  3. คำนวณดาเมจต่อเป้าด้วยสูตรร่วม (docs/01 §4) → emit floating damage พร้อมกัน (docs/11 §8)
- ยังมี safety cap `max_targets` (docs/11 §7) กันเคสฝูงมหาศาลเพื่อ performance/สมดุล
```python
def resolve_shape(caster, skill):
    cells = build_offsets(skill.shape, level=skill.level)  # §3 scaling (Lv1–100)
    cells = [rotate(o, skill.shape.orientation) for o in cells]
    world = [caster.cell + o for o in cells if in_bounds(caster.cell + o)]
    targets = [e for e in enemies_on(world) if e.alive][:skill.max_targets]
    for t in targets: apply_damage(t, ...)   # docs/01 §4
```

---

## 2. ทรงพื้นที่ทั้ง 6 แบบ

### แบบ 1 — เส้นตรงมีทิศ (Directional Line)
เส้นตรงพุ่งออกจากตัวละคร **เลือกทิศได้เอง** (หน้า/หลัง/ซ้าย/ขวา) ยาว `length` กว้าง `width`
```
        (length = 3, width = 1, ทิศ = "หน้า")
            . # .
            . # .
            . # .
            . C .
```
- offsets (ทิศหน้า): `(0,1),(0,2),(0,3)`  → หมุนตาม orientation เพื่อยิงหลัง/ซ้าย/ขวา
- `width>1` ขยายข้างเส้น (เช่น width 3 = เพิ่มคอลัมน์ซ้าย-ขวา)
- เหมาะวิชาพุ่งทะลุ/กระบี่ปาด เลือกแนวศัตรูที่เรียงกัน

### แบบ 2 — กากบาทเล็ก 5 ช่อง วางจุดเองได้ (Targeted Plus-5)
ทรงกากบาท 5 ช่อง (ช่องกลาง + 4 ช่องประชิด) แต่ **ผู้เล่นเลือก "จุดศูนย์กลาง" วางลงบนกริด
ได้เอง ภายในรัศมีจำกัดรอบตัวละคร** (`cast_range`) — ไม่ผูกกับตำแหน่งตัวละคร
```
   ขอบเขตเลือกจุดวาง (cast_range = 3, ◌ = ช่องที่เล็งวางได้)
   จุดที่เลือก = P → กากบาทกางรอบ P:
            . ◌ ◌ ◌ . . .              . # .
            ◌ ◌ ◌ ◌ ◌ . .              # P #     ← ทรงที่ลงจริงรอบจุด P
            ◌ ◌ ◌ ◌ ◌ . .              . # .
            ◌ ◌ C ◌ ◌ . .
            ◌ ◌ ◌ ◌ ◌ . .
```
- **cast_range:** เลือกจุดศูนย์กลาง `P` ที่ `grid_dist(C, P) <= cast_range` (docs/11 §3)
- offsets (เทียบจาก P): `(0,0),(0,1),(0,-1),(1,0),(-1,0)` → กางรอบจุดที่วาง
- ถ้าไม่เล็ง/auto-script: วางบนกระจุกศัตรูหนาสุดในรัศมีโดยอัตโนมัติ
- เหมาะวิชาเล็งจุด (เช่น ตบฝ่ามือ/ปาเข็มลงจุด) เคลียร์กระจุกศัตรูที่อยู่ห่างตัวได้

### แบบ 3 — กากบาทเต็มเส้น (Full Cross / Orthogonal Rays)
4 เส้นตรงพุ่งออก 4 ทิศตั้งฉาก จุดตัดที่ตัวละคร ยาวทิศละ `length`
```
            (length = 2)
            . . # . .
            . . # . .
            # # C # #
            . . # . .
            . . # . .
```
- offsets (length L): ทุก `(±i,0),(0,±i)` สำหรับ i=1..L
- เหมาะวิชาแผ่พลังตามแกน โดนศัตรูที่ขนาบ 4 ทิศ

### แบบ 4 — ดาวกระจาย 6 เส้น: กากบาท + แนวทแยง (6-Ray Star)
เหมือนแบบ 3 แต่เพิ่ม **เส้นแนวทแยง** พุ่งออกจากตัวละคร **รวม 6 เส้น** (ค่าเริ่มต้น)
```
            (length = 2, 6 เส้น = แกนตั้ง 2 + ทแยง 4)
            # . # . #
            . # # # .
            . . C . .
            . # # # .
            # . # . #
```
- ค่าเริ่มต้น 6 เส้น = แกนตั้ง (หน้า/หลัง) + ทแยงทั้ง 4 มุม → สมมาตรซ้าย-ขวา/หน้า-หลัง
  offsets (length L): `(0,±i)` + `(±i,±i)` สำหรับ i=1..L  (เว้นแกนนอน ซ้าย/ขวา)
- **`rays` ตั้งค่าได้** (data-driven): default = **6 เส้น** ตามดีไซน์; ปรับเป็น 8 ทิศ (สมมาตรเต็ม)
  หรือชุดทิศอื่นได้
  > หมายเหตุ: บนกริดสี่เหลี่ยม "6 เส้น" เลือกแกนตั้ง+ทแยงเพื่อคงความสมมาตร (แกนนอนเว้นไว้);
  > ถ้าใช้กริด **หกเหลี่ยม (hex)** 6 เส้นคือรูปแบบธรรมชาติของ hex อยู่แล้ว (docs/11 §3)
- เหมาะวิชาระเบิดพลังรอบตัวตามแนวรัศมี

### แบบ 5 — พีระมิด วางจุดเองได้ (Targeted Pyramid: 5×5 + Spikes)
พื้นที่ทึบ 5×5 + สามเหลี่ยมแหลมยื่น 4 ด้าน (ทรงเพชร/พีระมิดมองบน) แต่ **ผู้เล่นเลือก "จุด
ศูนย์กลาง" วางลงบนกริดได้เอง ภายในรัศมีจำกัดรอบตัว (`cast_range`)** เหมือนแบบ 2
```
        C=ตัวละคร, o=ช่องเล็งวางได้, #=พีระมิดที่กางรอบจุด P ที่เลือก
        . . . . . . # . . . .
        . o o o o # # # o . .
        . o o o # # # # # . .
        . o o # # # # # # # .
        . o # # # # # # # # #
        . o o # # # # # # # .
        . o C o # # # # # . .       ← วาง P ห่างตัวได้ แล้วพีระมิดกางรอบจุดนั้น
        . o o o # # # o . . .
        . o o o o # o . . . .
```
- **cast_range:** เลือกจุดศูนย์กลาง `P` ที่ `grid_dist(C, P) <= cast_range`
- offsets (เทียบจาก P): core 5×5 `x,y ∈ [-2,2]` + เงี่ยง (ระยะ 3 → กว้าง 3 ช่อง, ระยะ 4 → 1 ช่อง)
- ไม่เล็ง/auto-script: วางบนกระจุกศัตรูหนาสุดในรัศมีอัตโนมัติ
- เหมาะวิชาสุดยอดสายพื้นที่กว้าง ลงจุดกลางฝูงที่อยู่ห่างตัว — ครอบศัตรูหนาแน่นได้

### แบบ 6 — วงกว้างรอบตัว (Radial / Ultimate)
รัศมีวงกว้างรอบตัวละคร โดน **ทุกช่องในรัศมี R** (ท่าไม้ตายล้างฝูง — ตรงกับ docs/11 §7)
```
            (radius = 3, วัดแบบ Euclidean ให้ดูกลม)
            . . # # # . .
            . # # # # # .
            # # # # # # #
            # # # C # # #
            # # # # # # #
            . # # # # # .
            . . # # # . .
```
- offsets: ทุกช่อง `dist(C, cell) <= R` (เลือก metric ตาม grid mode — Chebyshev = วงสี่เหลี่ยม)
- คือ "สุดยอดวิชา" รัศมีกว้างสุด, max_targets สูงสุด

---

## 3. การเปลี่ยนแปลงตามระดับวิชา (Level Progression)
"ระดับวิชา" ส่งผลต่อทรงได้ **2 รูปแบบ** ใช้ร่วมกันได้:

> 📌 **สเกลหลัก = ระดับวิชา 1–100** (สอดคล้อง docs/02). หาก UI อยากแสดงเป็นดาวประดับ
> ใช้ 1 ดาว = 10 ระดับ ได้ แต่ค่าจริงในระบบคือ Lv. threshold ด้านล่างตั้งค่าได้อิสระต่อวิชา

### 3.1 เปลี่ยน "ชนิดทรง" ตามระดับ (Shape Morph by Level) ⭐ สำคัญ
บางวิชา **ทรงเปลี่ยนชนิด** เมื่อถึงระดับที่กำหนด (ไม่ใช่แค่ขยายขนาด) เช่นวิชาเดียวกัน:
```
ระดับ 1–29   → แบบ 1 (เส้นตรง)        . . . #
                                       . . . #
                                       . . . #
                                       . . . C

ระดับ 30–59  → แบบ 3 (กากบาทเต็มเส้น)  . . . # . . .
                                       . # # C # # .
                                       . . . # . . .

ระดับ 60–100 → แบบ 4 (ดาว 6 เส้น)      . # . # . # .
                                       . . # # # . .
                                       . . . C . . .
                                       . . # # # . .
                                       . # . # . # .
```
กำหนดด้วยฟิลด์ `shape_stages` (เรียงตามช่วงระดับ — ใช้ทรงของช่วงที่ระดับปัจจุบันตกอยู่):
```jsonc
"shape_stages": [
  { "min_level": 1,  "max_level": 29,  "type": "line",  "params": { "length": 3 }, "orientation": "front" },
  { "min_level": 30, "max_level": 59,  "type": "cross", "params": { "length": 2 } },
  { "min_level": 60, "max_level": 100, "type": "star",  "params": { "length": 2, "rays": 6 } }
]
```
> **เหตุผลเชิงดีไซน์:** ทรงเปลี่ยนชนิดตามระดับให้ความรู้สึก "วิชาวิวัฒน์/แตกฉาน" ชัดเจน —
> ผู้เล่นเห็นวิชาโปรดเปลี่ยนจากเส้นเดียวเป็นดาวกระจาย เป็นรางวัลการฝึกที่จับต้องได้

### 3.2 ขยาย "ขนาดทรงเดิม" ตามระดับ (Size Scaling)
วิชาที่ไม่เปลี่ยนชนิด แต่พารามิเตอร์โตขึ้น — กำหนดใน `shape.scaling`
**ตารางตัวอย่าง (เทียบช่วงระดับ Lv):**
| ทรง | พารามิเตอร์โต | Lv10 | Lv30 | Lv60 | Lv100 |
|---|---|---|---|---|---|
| 1 เส้นตรง | length / width | 3 / 1 | 4 / 1 | 5 / 3 | 7 / 3 |
| 3 กากบาท | length | 2 | 3 | 4 | 6 |
| 2 กากบาทวางจุด | cast_range | 2 | 2 | 3 | 4 |
| 4 ดาวกระจาย | length (6 เส้น) | 1 | 2 | 3 | 4 |
| 5 พีระมิด (วางจุด) | core/spike + cast_range | 3×3, cr3 | 5×5, cr4 | 5×5+, cr5 | 7×7, cr6 |
| 6 วงกว้าง | radius | 2 | 3 | 4 | 5 |
> แบบ 2 ทรงคงที่ 5 ช่อง — สเกลที่ **ระยะเลือกจุดวาง (cast_range)**; แบบ 5 สเกลทั้งขนาด+cast_range
> ดาเมจต่อช่องยังสเกลด้วย `skill_mult` (docs/01) แยกจากการเปลี่ยน/ขยายพื้นที่

---

## 4. ทิศทาง & การหมุน (Orientation)
- ทรงมีทิศ (แบบ 1) และทรงไม่สมมาตร เลือก `orientation`:
  - กริดสี่เหลี่ยม: `front / right / back / left` (หมุน 0°/90°/180°/270°)
  - ทรงรวมทแยง (แบบ 4) รองรับหมุน 45° เพิ่มได้
- **แบบ 2 และ 5 (วางจุด):** ไม่ใช้การหมุน แต่ "เล็งจุดศูนย์กลาง" บนกริดภายใน `cast_range`
- **วิธีเล็ง (UX มือถือ):** แบบมีทิศ→ปัดนิ้วกำหนดทิศ; แบบวางจุด→แตะช่องเป้าหมายในรัศมี;
  ทั้งคู่มีโหมด auto ล็อกไปยังกระจุกศัตรูหนาสุด
- ทรงสมมาตรรอบตัว (3,6) ไม่ต้องเล็ง — ปล่อยรอบตัวทันที (เหมาะ auto-script docs/01 §6)
```python
ROT = { "front":0, "right":90, "back":180, "left":270 }
def rotate(off, orientation):
    a = radians(ROT[orientation]); x,y = off
    return ( round(x*cos(a) - y*sin(a)), round(x*sin(a) + y*cos(a)) )
```

---

## 5. ส่วนขยาย Schema (ต่อจาก docs/10 §1 Skill Schema)
```jsonc
{
  "id": "cloud_cross_strike",
  "name": "กระบี่กากบาทเมฆ",
  "shape": {
    "type": "cross",            // line | plus5 | cross | star | pyramid | radial
    "params": { "length": 2, "width": 1, "radius": 0, "rays": 6, "cast_range": 0 },
    "orientation": "front",     // front|right|back|left (เฉพาะทรงมีทิศ)
    "scaling": { "length": [2,3,4,6] }  // index = ช่วงระดับ หรือสูตร string
  },
  // หรือใช้ shape_stages แทน shape เมื่อทรงเปลี่ยนชนิดตามระดับ (§3.1):
  // "shape_stages": [ {min_level,max_level,type,params,orientation}, ... ],
  "max_targets": 16,            // safety cap (docs/11 §7)
  "falloff": { "inner": 99, "outer_mult": 1.0 }  // ทรงเส้น/กากบาทมักไม่ลดตามระยะ
}
```
**แมประหว่าง type ↔ ตัวสร้าง offset:**
| type | พารามิเตอร์หลัก | สมมาตร/มีทิศ |
|---|---|---|
| `line` | length, width | มีทิศ |
| `plus5` | cast_range | วางจุดเอง (targeted) |
| `cross` | length | สมมาตร |
| `star` | length, rays (default 6) | สมมาตร (หมุน 45° ได้) |
| `pyramid` | core, spike, cast_range | วางจุดเอง (targeted) |
| `radial` | radius | สมมาตร |

---

## 6. การจับคู่ทรงกับสำนัก (ตัวอย่างดีไซน์)
| สำนัก | ทรงเด่น | เหตุผล |
|---|---|---|
| เขาเมฆขาว (กระบี่) | 1 เส้นตรง, 3 กากบาท | ปาดแนว/แทงทะลุ |
| ลมวสันต์ (ตัวเบา) | 1 เส้นตรง, 4 ดาวกระจาย | พุ่งเร็วหลายทิศ |
| กระบองเหล็ก (กาย) | 2 Plus-5, 6 วงกว้าง(ทุบพื้น) | กระแทกรอบตัว |
| หุบเขายาพิษ (พิษ) | 5 พีระมิด, 6 วงกว้าง | แผ่หมอกพิษพื้นที่ |
| วารีบูรพา (ฝ่ามือ) | 3 กากบาท(ฟื้น/บัฟแนว), 6 วงกว้าง(ฟื้นทีม) | utility แนว/รอบตัว |
> ทรงเดียวกันใช้กับวิชาฟื้น/บัฟได้ (เปลี่ยน effect จาก damage → heal/buff, เลือกเป้าเป็นพวกเดียวกัน)

---

## 7. หมายเหตุการ implement
- เก็บ "ทรง" เป็น **ตาราง offset ที่ pre-compute ต่อ (type, level, orientation)** → เร็ว ไม่คำนวณซ้ำทุก tick
- ตรวจขอบกริด/สิ่งกีดขวาง: ช่องที่ตกนอกสนาม/ติด blocked ตัดออก (หรือให้เส้นถูกบล็อกตามดีไซน์)
- ไฮไลต์ช่องที่จะโดน (telegraph) ก่อนปล่อยท่าหนัก → ทั้งผู้เล่นเล็งและศัตรูหลบ (docs/11 §9)
- ทรง 5,6 ระดับสูง = เป้าจำนวนมาก → ใช้ batched damage events (docs/11 §8) + object pooling floating text
